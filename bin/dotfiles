#!/usr/bin/env bash

BIN_NAME=$(basename "$0")
COMMAND_NAME=$1

ARROW="￫"
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Get dotfiles dir (so run this script from anywhere)
export DOTFILES_DIR EXTRA_DIR
DOTFILES_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )"

sub_help () {
    # Gold-gradient wordmark (banner.py is canonical); never let it break help.
    uv run --project "$DOTFILES_DIR/cli" python -c "from dotfiles.banner import print_banner; print_banner()" 2>/dev/null || true
    printf "%s Usage: ${YELLOW}%s <command>${NC}\n" "$ARROW" "$BIN_NAME"
    printf "\n"
    printf "   help                 This help message\n"
    printf "\n"
    printf "${BLUE}Machine${NC} — setup, maintenance, and machine-state\n"
    printf "   install              Run full dotfiles setup\n"
    printf "   update               Update OS, Homebrew, runtimes, and dev tools\n"
    printf "   doctor [--fix]       Check all tools are installed (--fix to repair config)\n"
    printf "   brew install [opts]  Sync packages from packages.toml (--dry-run, --no-ai/-productivity/-social)\n"
    printf "   brew upgrade         Upgrade all installed packages (brew is the version surface)\n"
    printf "   brew stale           Find installed packages not declared in packages.toml\n"
    printf "   clean                Clean up Homebrew caches\n"
    printf "   dock                 Reset macOS Dock layout\n"
    printf "   snapshot [ls|diff]   Capture / list / diff machine-state snapshots\n"
    printf "   typewhisper <cmd>    Manage TypeWhisper config (status|apply)\n"
    printf "   profile-shell        Profile shell startup time\n"
    printf "\n"
    printf "${BLUE}Control${NC} — drive this Mac locally or from your phone\n"
    printf "   tui                    Launch the Mission Control TUI (phone-drivable dashboard)\n"
    printf "   remote on [opts]       SSH/Mosh/Zellij access for Termius (--add-key, --harden-ssh, --session, --dry-run)\n"
    printf "   remote off [opts]      Turn off macOS Remote Login (--kill-sessions, --dry-run)\n"
    printf "   remote status          Show the remote-shell entrypoint state\n"
    printf "   session <cmd>          zellij sessions: ls, attach, new, kill, or bare picker\n"
    printf "\n"
    printf "${BLUE}AI${NC} — agentic tooling and local models\n"
    printf "   agent global setup [opts]        Configure all AI vendors (--reset-mcp, --clean)\n"
    printf "   agent global overview            Show active agentic setup (dashboard)\n"
    printf "   agent global lint                Validate skill/agent files against conventions\n"
    printf "   agent global verify [--offline]  Check skills/agents deployed + probe MCP servers\n"
    printf "   agent local [path]               Align a repo's agent instructions (--check, --vendors)\n"
    printf "   agent web copy [--step|--list]   Copy web-chat instructions to clipboard (Gemini)\n"
    printf "   benchmark <cmd>                  Benchmark local LM Studio models (list|bench|estimate|compare)\n"
}

sub_update () {
    # shellcheck source=../macos/print_utils.sh
    source "$DOTFILES_DIR/macos/print_utils.sh"

    print_header "Updating everything"

    # --- macOS ---
    print_section "macOS system updates"
    if sudo softwareupdate -i -a 2>&1 | tail -1; then
        print_success "macOS up to date"
    else
        print_warn "macOS update finished with warnings (check above)"
    fi

    # --- Homebrew ---
    print_section "Homebrew (formulae + casks)"
    brew update
    if brew upgrade 2>&1; then
        print_success "Homebrew packages up to date"
    fi
    # Quiet cleanup: drop the "Skipping/Removing" wall, keep the freed summary.
    local freed
    freed="$(brew cleanup --prune=30 2>/dev/null | grep -o 'freed approximately.*' || true)"
    print_dim "Cleaned caches older than 30 days${freed:+ — $freed}"

    # --- Node.js (fnm) ---
    if command -v fnm >/dev/null 2>&1; then
        print_section "Node.js (fnm)"
        eval "$(fnm env)"
        fnm install --lts
        fnm use --install-if-missing lts-latest
        fnm default lts-latest
        print_success "Node.js LTS: $(node --version)"
    fi

    # --- Bun ---
    if command -v bun >/dev/null 2>&1; then
        print_section "Bun"
        bun upgrade
        print_success "Bun: $(bun --version)"
    fi

    # --- Rust (rustup) ---
    if command -v rustup >/dev/null 2>&1; then
        print_section "Rust (rustup)"
        rustup update
        print_success "Rust: $(rustc --version | awk '{print $2}')"
    fi

    # --- UV (Python) ---
    if command -v uv >/dev/null 2>&1; then
        print_section "UV (Python package manager)"
        uv self update
        print_success "UV: $(uv --version)"
    fi

    # --- Global npm packages ---
    if command -v npm >/dev/null 2>&1; then
        print_section "Global npm packages"
        npm update -g
        print_success "Global npm packages updated"
    fi

    # --- Go tools ---
    if command -v go >/dev/null 2>&1; then
        print_section "Go tools"
        local go_tools=(
            "golang.org/x/tools/gopls@latest"
            "github.com/go-delve/delve/cmd/dlv@latest"
            "github.com/air-verse/air@latest"
            "github.com/sqlc-dev/sqlc/cmd/sqlc@latest"
            "github.com/pressly/goose/v3/cmd/goose@latest"
            "github.com/a-h/templ/cmd/templ@latest"
            "honnef.co/go/tools/cmd/staticcheck@latest"
        )
        for tool in "${go_tools[@]}"; do
            local tool_name
            tool_name=$(basename "${tool%%@*}")
            go install "$tool" 2>/dev/null && print_success "$tool_name" || print_warn "Failed: $tool_name"
        done
    fi

    print_completion "All updates finished"
}

sub_clean () {
    # shellcheck source=../macos/print_utils.sh
    source "$DOTFILES_DIR/macos/print_utils.sh"

    print_header "Cleaning up"

    print_section "Homebrew"
    # brew cleanup is noisy: per-file "Removing:" lines plus "Warning: Skipping X:
    # most recent version not installed" (expected — an older bottle whose newest
    # isn't installed). Drop stderr, surface only the freed-space summary.
    local freed
    freed="$(brew cleanup --prune=30 2>/dev/null | grep -o 'freed approximately.*' || true)"
    print_success "Cleared caches older than 30 days${freed:+ — $freed}"

    print_completion "Cleanup finished"
}

sub_dock () {
    . "$DOTFILES_DIR/macos/dock.sh"
    printf "${GREEN}%b Success! MacOS dock command finished.${NC}\n" "$ARROW"
}

sub_install() {
    . "$DOTFILES_DIR/install.sh"
}

sub_typewhisper() {
    "$DOTFILES_DIR/macos/typewhisper.sh" "$@"
}

sub_shell_profile() {
    printf "${BLUE}%b Profiling shell startup...${NC}\n\n" "$ARROW"
    # Time total startup
    local total
    total=$( { time zsh -i -c exit 2>&1; } 2>&1 | grep real | awk '{print $2}')
    printf "  Total startup time: ${BOLD}%s${NC}\n\n" "$total"

    # Detailed breakdown using zsh XTRACE
    printf "  ${BLUE}Top 10 slowest operations:${NC}\n"
    ZSHRC_PROFILE=1 zsh -i -c exit 2>&1 | \
        awk '{ gsub(/^[0-9]+\.[0-9]+/, "", $0); print }' 2>/dev/null || true

    # Simpler approach: time individual sourced files
    printf "\n  ${BLUE}File-by-file timing:${NC}\n"
    for f in ~/.zshenv ~/.zprofile ~/.zshrc; do
        if [[ -f "$f" ]]; then
            local t
            t=$( { time zsh -c "source $f" 2>/dev/null; } 2>&1 | grep real | awk '{print $2}')
            printf "  %-30s %s\n" "$(basename "$f")" "$t"
        fi
    done
    printf "\n"
    printf "  ${YELLOW}Tip:${NC} If startup is slow, check for heavy evals (fnm env, brew shellenv).\n"
}

# --- Namespace dispatchers ---
ns_shell() {
    local sub="${1:-profile}"; shift || true
    case "$sub" in
        profile) sub_shell_profile "$@" ;;
        *) printf "${RED}%b Unknown: shell %s${NC} (try: profile)\n" "$ARROW" "$sub" >&2; exit 1 ;;
    esac
}

# Python CLI (P1a + P1b). Migrated commands are delegated to uv run.
# As later phases land, add their names to this list.
# INVARIANT: every name here must also appear in sub_help and the zsh completion
# file (shell/completions/_dotfiles) — enforced by app/test_command_tree.py so
# the lists can't silently drift.
PY_CLI_COMMANDS=" remote session doctor benchmark brew snapshot tui "
if [[ -n "${1:-}" && "$PY_CLI_COMMANDS" == *" $1 "* ]]; then
    exec uv run --project "$DOTFILES_DIR/cli" dotfiles "$@"
fi

# agent: every subcommand is Python now — global (this machine), local (a repo), web (browser chats).
# Bare `agent` defaults to the global overview dashboard, matching the old dispatcher.
if [[ "${1:-}" == "agent" ]]; then
    shift
    [[ $# -eq 0 ]] && exec uv run --project "$DOTFILES_DIR/cli" dotfiles agent global overview
    exec uv run --project "$DOTFILES_DIR/cli" dotfiles agent "$@"
fi

case $COMMAND_NAME in
    "" | "-h" | "--help") sub_help ;;

    # Top-level
    install)     sub_install ;;
    update)      sub_update ;;
    clean)       sub_clean ;;
    dock)        sub_dock ;;

    # profile-shell is a canonical flat command (Machine group in sub_help)
    profile-shell) shift; sub_shell_profile "$@" ;;

    # Silent back-compat for the old `shell profile` single-item namespace
    shell)  shift; ns_shell "$@" ;;

    *)
        if declare -f "sub_${COMMAND_NAME}" >/dev/null 2>&1; then
            shift
            "sub_${COMMAND_NAME}" "$@"
        else
            printf "${RED}%b Error: '%s' is not a known command.${NC}\n" "$ARROW" "$COMMAND_NAME" >&2
            sub_help
            exit 1
        fi
        ;;
esac
