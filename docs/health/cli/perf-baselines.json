{
  "scope": "cli",
  "machine": "darwin-arm64 (dev)",
  "updated": "2026-06-09",
  "policy": "Perf-budget ratchet \u2014 baseline_ms lowers only (speedups lock in); a regression past tolerance_pct needs a justified bump. Numbers are ENVIRONMENT-SPECIFIC: re-baseline with `just perf --update` on the target machine/runner. NOT a pre-commit gate (benchmarks are slow + noisy) \u2014 run `just perf` manually or nightly.",
  "tolerance_pct": 25,
  "warmup": 3,
  "runs": 10,
  "benchmarks": [
    {
      "name": "agent-catechism",
      "command": "dotfiles agent catechism",
      "baseline_ms": 171.8
    },
    {
      "name": "agent-help",
      "command": "dotfiles agent --help",
      "baseline_ms": 178.7
    }
  ]
}
