# Code Health Report — cli/ — 2026-06-07

> Graded against `ai/skills/review/references/health-rubric.md` (U1–U11 universal + P1–P3 Python). Baseline snapshot; future passes diff against this. Numbers: [`baselines.json`](baselines.json). Open/tolerated items: [`findings.md`](findings.md).

## Executive summary

The `dotfiles` CLI + Mission Control TUI is a well-architected, moderately complex Python package. Strengths: **boundary discipline** (Pydantic at entries, ports/adapters), deliberate **consolidation** (single VENDORS registry deriving downstream uses), **focused modules** with clear hexagonal seams. Primary concerns: a few files over 400 LOC, **sparse observability** (structlog wired but barely used), and deferred type-boundary work at settings call sites.

## Universal criteria (U1–U11)

| # | Criterion | Grade | Note |
|---|---|---|---|
| U1 | Type Safety (1.5×) | B+ | typed `Agent`/`Vendor`, Pydantic at boundaries; `_JsonDict` load-bearing at JSON edges |
| U2 | Enum Discipline | A− | single VENDORS registry; Literals could be Enums for exhaustive match |
| U3 | DRY / Consolidation (1.5×) | B+ | shared merge/lib helpers; vendor modules separate by design (ADR-0001) |
| U4 | Module Focus | B | 5 files > 400 LOC mix concerns; functions within stay focused |
| U5 | Observability | C+ | structlog configured but only 5 files use it; core logic unlogged |
| U6 | Test Quality | B+ | ~10k test LOC; behavior-focused; integration/TUI coverage light |
| U7 | Error Handling | B | specific exceptions + graceful defaults; a few silent broad catches |
| U8 | Dead Code | A− | no unused imports/commented code; ignores justified |
| U9 | Boundary Contracts (1.5×) | B+ | Pydantic at entries; settings still dict-typed at some call sites |
| U10 | Concurrency (1.25×) | B+ | bounded `@work(exclusive=True)`, correct `call_from_thread`; no timeouts on slow calls |
| U11 | Module Depth (1.25×) | B+ | deep services behind small interfaces (`.run()`/`.report()`/`.overview()`) |
| P1 | Decorator Adoption | C+ | `@work`/`@cached_property` used; no retry/timing decorators on slow ops |
| P2 | Import Hygiene | A | `from __future__ import annotations` widespread; clean TYPE_CHECKING |
| P3 | Type Strictness | B− | 16 type:ignore, dict[str,Any] at JSON edges (load-bearing); no pyright-strict config |

**Weighted overall: 3.37 → B+.**

## Structural anti-pattern scan

No god functions (longest ~65 LOC), no arrow code (early returns; max ~4 nesting in a TUI modal), no lying signatures, no leaky boundaries (services return typed domain models), illegal states blocked by frozen Pydantic + Literals. Temporal coupling: none found (setup steps composable). Parse-don't-validate and humble-object both well applied. Rule-of-three: vendor setup pattern appears 5× — deliberately not consolidated (ADR-0001); revisit past a 6th vendor.

**AI-slop scan:** clean — domain types over primitives, shared logic extracted, no leaked external shapes, error paths present. `SpecialInstaller.extra: dict[str, Any]` is reasonable speculative metadata, not over-engineering.

## Strengths worth preserving

1. **Hexagonal discipline** — `ProcessRunner`/`HttpClient` ports, adapters replaceable, fakes injected in tests; `lib.py` explicitly bans `Path.home()`.
2. **Single-source VENDORS registry** — all vendor metadata derives from one place.
3. **Pydantic boundary design** — `extra="ignore"`, frozen models, validation centralized in `.load()`.

## Top opportunities (→ tracked in findings.md backlog)

1. **Observability** (U5/P1, lowest scores) — add structured logging at service entry/error points. Biggest single grade lever.
2. **Settings typed-boundary at call sites** (U9/P3) — refine `dict` → `Settings`; clears most of the cast/Any cluster.
3. **File-size extraction** (U4) — split `brew/service.py`, `agent/cli.py`.

## Calibration

> "Clean, well-architected infrastructure with good boundary discipline and solid tests. Five files over 400 LOC want a refactor pass for headroom. Observability is the weak spot — structlog is wired but barely used; add spans at service entry points and you're at A−. Vendor modules are intentionally separate (wise); consolidate only past a sixth. Overall: production-ready with minor, well-understood debt."

## Final: **B+** — production-ready; observability + file-size extraction would reach A−.
